let message = paseInt(prompt("Enter the maximum number!"));
while (!maximum) {
    maximum = parseInt(promt("Enter a valid number!"));
}
const targetNum = Math.florr((Math.random() * maximum)) + 1;

